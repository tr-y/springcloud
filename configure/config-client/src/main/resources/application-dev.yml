spring:
  application:
    name: configure-client
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8005

management:
  endpoints:
    web:
      exposure:
        include:  refresh

