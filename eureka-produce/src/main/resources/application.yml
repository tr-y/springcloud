spring:
  application:
    name: producer
  profiles:
    active: dev

eureka:
  instance:
    hostname: peer1
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://peer2:8088/eureka/


